% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_penguin_model.R
\name{fit_penguin_model}
\alias{fit_penguin_model}
\title{Perform EDA and prepare penguin data for modeling}
\usage{
fit_penguin_model(
  input_path = "data/01_clean_penguins.rds",
  output_path = "results/02_model_ready.rds",
  plot_path = "results/boxplot.png"
)
}
\arguments{
\item{input_path}{A string specifying the path to the cleaned \code{.rds} file (default: \code{"data/01_clean_penguins.rds"}).}

\item{output_path}{A string specifying where to save the processed modeling dataset (default: \code{"results/02_model_ready.rds"}).}

\item{plot_path}{A string specifying where to save the EDA boxplot (default: \code{"results/boxplot.png"}).}
}
\value{
Invisibly returns the prepared data frame for modeling.
}
\description{
This function reads the cleaned penguin dataset, performs exploratory data analysis
(summary stats and boxplot visualization), saves a figure to the \verb{results/} directory,
and returns the dataset prepared for modeling (selected features and factor conversion).
}
\examples{
fit_penguin_model()
}
